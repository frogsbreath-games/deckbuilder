import * as React from "react";
import { connect } from "react-redux";

const Game = () => (
  <div>
    <h1>Layout</h1>
    <p>Test Layout will go here</p>
  </div>
);

export default connect()(Game);
